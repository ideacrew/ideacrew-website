---
import { LOGO_IMAGE, SITE } from "@config";
import Hr from "./Hr.astro";
import Socials from "./Socials.astro";
import { Image } from "astro:assets";
import logoWhite from "/public/assets/logo-white.svg";

const currentYear = new Date().getFullYear();

export interface Props {
  noMarginTop?: boolean;
}

const { noMarginTop = false } = Astro.props;
---

<footer
  class={`${noMarginTop ? "" : "mt-auto"} w-full min-w-full bg-[#0B2441] sm:h-[210px]`}
>
  <div class="mx-auto h-full w-full max-w-[960px] px-4 py-8 sm:py-4">
    <div
      class="flex h-full w-full flex-col items-center text-white sm:flex-row sm:items-center sm:justify-between"
    >
      <div class="flex flex-col items-center gap-4 sm:items-start">
        <a href="/" class="logo">
          <Image
            src={logoWhite}
            alt="IdeaCrew Logo"
            width={245.42}
            height={65.88}
            loading="lazy"
          />
        </a>
        <div class="flex w-full justify-between text-xs">
          <div>{SITE.title}</div>
          <div>Copyright © {currentYear}</div>
        </div>
      </div>
      <nav class="mt-8 sm:mt-0">
        <ul class="flex flex-col items-center gap-6 text-sm sm:flex-row">
          <li>
            <a href="/" class="hover:text-skin-accent">Home</a>
          </li>
          <li>
            <a href="/about" class="hover:text-skin-accent">About</a>
          </li>
          <li>
            <a href="/work" class="hover:text-skin-accent">Work</a>
          </li>
          <li>
            <a href="/contact" class="hover:text-skin-accent">Contact</a>
          </li>
          <li>
            <a href="/careers" class="hover:text-skin-accent">Careers</a>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</footer>
